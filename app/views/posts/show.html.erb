<div class="row">
  <div class="col-lg-8 col-lg-offset-2">
    <%= @post.title %><br>
    <%= @post.created_at %><br>
    <%= link_to @post.user.email, user_path(@post.user) %>
    <div class="markdown">
      <%= markdown @post.body %>
    </div>
  </div>
</div>
